let formatted = true;

if (formatted) {
    console.log('The code is easy to read');
}

/*
Summary:
--------
- Use whitespace, including carriage return, space, newline, and tab, to format the code. The JavaScript engine ignores the whitespace.
- Use a semicolon (;) to terminate a simple statement.
- Use curly braces ({}) to form a block that groups one or more simple statements.
- A single-line comment starts with // followed by text.
- A block comment begins with /* * and ends with /
- Identifiers are names that you choose for variables, functions, classes, etc. 
- Do not use reserved keywords and reserved words for identifiers.
*/